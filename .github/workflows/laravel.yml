name: Laravel

on:
  push:
    branches: [ "main" ]

jobs:
  laravel-tests:

    runs-on: self-hosted

    steps:
    - run: cd ~/PROYECTO-INVENTARIO-WEB
    - name: Moverse al directorio
    - name: Descargar los cambios del repo
    - run: git pull
